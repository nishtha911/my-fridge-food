# Copy this file to `.env` inside `server/` and fill values
DB_USER=your_db_user
DB_HOST=your_db_host
DB_NAME=your_db_name
DB_PASSWORD=your_db_password
DB_PORT=5432

# Optional: set to 'true' to serve client build from server
SERVE_STATIC=false
